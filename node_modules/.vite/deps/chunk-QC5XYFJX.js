import {
  init_scrollreveal_es,
  scrollreveal_es_default
} from "./chunk-PKAP2FYJ.js";

// ../node_modules/vue-scroll-reveal/dist/vue-scroll-reveal.esm.js
init_scrollreveal_es();
function reveal(el, value, modifiers) {
  const options = value || {};
  if (modifiers) {
    if (modifiers.reset) {
      options.reset = true;
    }
    if (modifiers.nomobile) {
      options.mobile = false;
    }
    if (modifiers.nodesktop) {
      options.desktop = false;
    }
  }
  if (typeof options.class === "string") {
    el.classList.add(options.class);
    delete options.class;
  }
  scrollreveal_es_default().reveal(el, options);
}
var vScrollReveal = {
  mounted(el, { value, modifiers }) {
    reveal(el, value, modifiers);
  },
  updated(el, { value, modifiers }) {
    reveal(el, value, modifiers);
  },
  getSSRProps() {
    return {};
  }
};
function createScrollRevealDirective(defaultOptions) {
  return {
    mounted(el, { value, modifiers }) {
      reveal(el, Object.assign({}, defaultOptions, value), modifiers);
    },
    updated(el, { value, modifiers }) {
      reveal(el, Object.assign({}, defaultOptions, value), modifiers);
    },
    getSSRProps() {
      return {};
    }
  };
}

export {
  vScrollReveal,
  createScrollRevealDirective
};
//# sourceMappingURL=chunk-QC5XYFJX.js.map
